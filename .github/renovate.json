{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "enabledManagers": ["regex"],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^version\\.env$/"
      ],
      "matchStrings": [
        "VERSION=(?<currentValue>.*?)"
      ],
      "depNameTemplate": "element-hq/element-web",
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v(?<version>.*)$"
    }
  ]
}
